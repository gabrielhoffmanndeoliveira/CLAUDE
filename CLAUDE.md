# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`gabrielhoffmanndeoliveira/CLAUDE`) is currently empty — no commits, no source files, and no configuration exist yet. There is no build system, test suite, or architecture to document.

## Next steps

Once code is added to this repository, update this file to cover:

- **Commands**: how to install dependencies, build, lint, run tests (including running a single test), and start the app locally.
- **Architecture**: the high-level structure of the codebase — major modules/services, how they interact, and any non-obvious design decisions that span multiple files.
- **Conventions**: project-specific conventions not already enforced by linters/formatters.

Re-run repository analysis after the initial code is committed so this file reflects the real project rather than a placeholder.
