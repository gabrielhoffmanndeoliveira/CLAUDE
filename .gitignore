# Scratch scripts generated by research agents while enriching product copy.
# These only emit JSON into the session scratch area; they are not project code.
tfas_*_build.py

# Manufacturer datasheets downloaded by research agents for verification.
# Reference material, never project content.
*.pdf
